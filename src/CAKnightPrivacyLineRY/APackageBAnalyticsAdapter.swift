#if canImport(AdjustSdk)
import AdjustSdk
#endif
import FBSDKCoreKit
import Foundation
import UIKit

final class APackageBAnalyticsAdapter: CAInGliderCryRY {
    static let CABootHousePotionRY = APackageBAnalyticsAdapter()

    private let CADarlingFromOtherRY = NSLock()
    private var CATootsyStudentScreenRY = CAChairDearSlowRY.CABootHousePotionRY.CACloudTrialsInfoRY
    private var CALaterPrinceUnevenlyRY = ""
    private var CARandomClockLineRY = false

    private init() {}

    var CACloudTrialsInfoRY: String {
        CADarlingFromOtherRY.lock()
        defer { CADarlingFromOtherRY.unlock() }
        return CATootsyStudentScreenRY
    }

    var CAResWinDialectRY: String {
        CADarlingFromOtherRY.lock()
        defer { CADarlingFromOtherRY.unlock() }
        return CALaterPrinceUnevenlyRY
    }

    func CAAnyJumpMuchRY() async -> String {
        let CARedStreetCityRY = CACloudTrialsInfoRY
        if !CARedStreetCityRY.isEmpty {
            return CARedStreetCityRY
        }

        #if canImport(AdjustSdk)
        for CABindPaintDependRY in 1...3 {
            let CASauceHeroConcentrateRY: String = await withCheckedContinuation { CABigStreetWhaleRY in
                Adjust.adid { CACopperMoleBigRY in
                    CABigStreetWhaleRY.resume(
                        returning: (CACopperMoleBigRY ?? "").trimmingCharacters(in: .whitespacesAndNewlines)
                    )
                }
            }
            if !CASauceHeroConcentrateRY.isEmpty {
                CAForgetSwapZeroRY(CASauceHeroConcentrateRY)
                return CASauceHeroConcentrateRY
            }
            if CABindPaintDependRY < 3 {
                try? await Task.sleep(nanoseconds: 500_000_000)
            }
        }
        #endif

        let CAVcDialogueDelRY = CACloudTrialsInfoRY
        return CAVcDialogueDelRY
    }

    func CABlindUpAirRY(CAMuchNightRunsRY: ADJAttribution?, CACopperMoleBigRY: String) {
        let CAAversionMagicFireRY = CAGrapeJumpMonkeyRY(CAMuchNightRunsRY)
        CAForgetSwapZeroRY(CACopperMoleBigRY)
        CADarlingFromOtherRY.lock()
        CALaterPrinceUnevenlyRY = CAAversionMagicFireRY
        CADarlingFromOtherRY.unlock()

    }

    private func CAForgetSwapZeroRY(_ CACopperMoleBigRY: String) {
        let CACountZeroSubRY = CACopperMoleBigRY.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !CACountZeroSubRY.isEmpty else { return }
        CADarlingFromOtherRY.lock()
        CATootsyStudentScreenRY = CACountZeroSubRY
        CADarlingFromOtherRY.unlock()
        CAChairDearSlowRY.CABootHousePotionRY.CACloudTrialsInfoRY = CACountZeroSubRY
    }

    func CAGrapeJumpMonkeyRY(_ CAMuchNightRunsRY: ADJAttribution?) -> String {
        guard let CAMuchNightRunsRY else { return "" }
        let CADirectorOctopusShieldRY: [String: Any] = [
            "trackerToken": CAMuchNightRunsRY.trackerToken ?? "",
            "trackerName": CAMuchNightRunsRY.trackerName ?? "",
            "network": CAMuchNightRunsRY.network ?? "",
            "campaign": CAMuchNightRunsRY.campaign ?? "",
            "adgroup": CAMuchNightRunsRY.adgroup ?? "",
            "creative": CAMuchNightRunsRY.creative ?? "",
            "clickLabel": CAMuchNightRunsRY.clickLabel ?? "",
            "costType": CAMuchNightRunsRY.costType ?? "",
            "costAmount": CAMuchNightRunsRY.costAmount ?? 0,
            "costCurrency": CAMuchNightRunsRY.costCurrency ?? ""
        ]
        guard JSONSerialization.isValidJSONObject(CADirectorOctopusShieldRY),
              let CAGliderDevEnhanceRY = try? JSONSerialization.data(withJSONObject: CADirectorOctopusShieldRY, options: [.sortedKeys]) else {
            return ""
        }
        return String(decoding: CAGliderDevEnhanceRY, as: UTF8.self)
    }

    func CAInfoBessMouseRY(_ CADevastateHoorayMinRY: CAThanMorningClothesRY,
                                  CALiefMoleAlterRY: Decimal?,
                                  CANightAnimalAnyRY: String?) {
        let CAComplainLovingDialectRY: String
        switch CADevastateHoorayMinRY {
        case .CADelRainDorsalRY:
            CAComplainLovingDialectRY = CAReadFishZeroRY.CAjoinIndexMonkeyRY
        case .CAInsectDetectTracksRY:
            return
        case .CACollapseDemonstrateBessRY:
            CAComplainLovingDialectRY = CAReadFishZeroRY.CABorderDisagreeBottleRY
        }

        guard let CAShieldBlightDarlingRY = ADJEvent(eventToken: CAComplainLovingDialectRY) else { return }
        if CADevastateHoorayMinRY == .CACollapseDemonstrateBessRY {
            guard let CALiefMoleAlterRY,
                  let CANightAnimalAnyRY,
                  !CANightAnimalAnyRY.isEmpty else { return }
            CAShieldBlightDarlingRY.setRevenue(
                NSDecimalNumber(decimal: CALiefMoleAlterRY).doubleValue,
                currency: CANightAnimalAnyRY
            )
        }
        Adjust.trackEvent(CAShieldBlightDarlingRY)
    }

    @MainActor
    func CAPickColonialBirthRY(
        CALaptopSwapWindowRY: UIApplication,
        CAIdahoUntilSixRY: [UIApplication.LaunchOptionsKey: Any]?
    ) {
        guard !CARandomClockLineRY else { return }
        let CAArgDimensionCeillingRY = CAChairDearSlowRY.CABootHousePotionRY
        if CAArgDimensionCeillingRY.CALineIceDisagreeRY.isEmpty {
            CAArgDimensionCeillingRY.CALineIceDisagreeRY = CAReadFishZeroRY.CALineIceDisagreeRY
        }
        if CAArgDimensionCeillingRY.CATwoSixElephantRY.isEmpty {
            CAArgDimensionCeillingRY.CATwoSixElephantRY = CAReadFishZeroRY.CATwoSixElephantRY
        }
        if CAArgDimensionCeillingRY.CACoatScabForgetRY.isEmpty {
            CAArgDimensionCeillingRY.CACoatScabForgetRY = CAReadFishZeroRY.CACoatScabForgetRY
        }

        let CAVineDegreeCallRY = Settings.shared
        CAVineDegreeCallRY.appID = CAArgDimensionCeillingRY.CALineIceDisagreeRY
        CAVineDegreeCallRY.clientToken = CAArgDimensionCeillingRY.CATwoSixElephantRY
        CAVineDegreeCallRY.displayName = CAArgDimensionCeillingRY.CACoatScabForgetRY
        CAVineDegreeCallRY.isAutoLogAppEventsEnabled = true
        ApplicationDelegate.shared.application(
            CALaptopSwapWindowRY,
            didFinishLaunchingWithOptions: CAIdahoUntilSixRY
        )
        CARandomClockLineRY = true
    }

    func CAOilReadFlowerRY(CALiefMoleAlterRY: Decimal, CANightAnimalAnyRY: String) {
        guard CARandomClockLineRY else { return }
        AppEvents.shared.logPurchase(
            amount: NSDecimalNumber(decimal: CALiefMoleAlterRY).doubleValue,
            currency: CANightAnimalAnyRY,
            parameters: [AppEvents.ParameterName("fb_mobile_purchase"): "true"]
        )
    }
}
